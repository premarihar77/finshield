# FinShield Frontend

React + Vite frontend for FinShield.

## Run

```bash
cd frontend
npm install
copy .env.example .env
npm run dev
```

The app expects the backend at `http://localhost:8000` unless `VITE_API_BASE_URL` is changed.
